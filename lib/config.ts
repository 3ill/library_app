const config = {
  env: {
    urlEndpount: process.env.NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT,
    publicKey: process.env.NEXT_PUBLIC_IMAGEKIT_URL_PUBLIC_KEY,
    privateKey: process.env.IMAGE_KIT_PRIVATE_KEY,
  },
};

export default config;
